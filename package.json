{
  "name": "specable2-workspace",
  "private": true,
  "scripts": {
    "docker:build": "docker compose build",
    "docker:claude": "docker compose run --rm claude-code",
    "docker:shell": "docker compose run --rm claude-code bash"
  }
}
